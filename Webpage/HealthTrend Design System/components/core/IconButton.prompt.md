Square icon-only control for chart and header toolbars, where a text label would compete with data.

\`\`\`jsx
<IconButton icon="settings-2" label="Chart settings" />
\`\`\`

Always pass \`label\`. Use only in toolbars — never as the primary action of a view.
