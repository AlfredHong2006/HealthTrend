Renders one Lucide glyph at the system stroke weight; use it for every icon rather than pasting SVG.

\`\`\`jsx
<Icon name="trending-down" size={16} color="var(--text-qualifier)" />
\`\`\`

Icons are always monochrome and inherit meaning from position, never from colour. Never use an icon to signal good/bad.
