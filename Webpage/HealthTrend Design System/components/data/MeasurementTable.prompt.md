Tier-3 ledger of raw readings beside the model's estimate. Hairline rows only — no card, no zebra striping, no side borders.

    <MeasurementTable rows={rows} unit="kg" />

Missing values render as an em dash; a gap in measurement is information, not an error.
