Wraps every run of language in the product and in the Method section.

\`\`\`jsx
<Prose>
  <p>A scale measures one morning. It does not measure a trajectory.</p>
  <p>The filter separates the two: a slow state, and the noise around it.</p>
</Prose>
\`\`\`

Never set prose wider than \`--measure-prose\` (34em) or narrower than 24em.
