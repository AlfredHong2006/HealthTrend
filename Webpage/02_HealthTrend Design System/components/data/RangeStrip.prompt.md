Whole-history context above or below the trajectory chart, with the visible window highlighted; click to move the window.

    <RangeStrip series={allDays} from={0.62} to={1} ticks={['2024','2025','2026']} onChange={setWindow} />

Inside the window the area is azure; outside it is neutral grey. Never the only range control — pair it with a SegmentedControl.
