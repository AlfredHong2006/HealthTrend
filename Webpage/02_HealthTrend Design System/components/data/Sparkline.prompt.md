Axis-free micro-trend for rows, headers and nav — always the estimate, never raw readings.

    <Sparkline trend={last30} width={132} height={38} />

No dots, no labels, no axis. If it needs a number, put the number beside it in tier 2 or 3.
