Current rate of change of the trajectory, with its interval.

\`\`\`jsx
<TrendDelta value="−0.42" direction="down" interval="95% CI −0.61 to −0.23 kg/week" />
\`\`\`

Use \`direction="flat"\` whenever the interval crosses zero — the model cannot resolve a direction, so the UI must not imply one.
