"""HealthTrend backend.

Milestone 1 contains a single layer: :mod:`app.core`, the pure numerical core.
Later milestones add ``ingestion``, ``services``, ``schemas`` and ``api`` packages
alongside it. See ``docs/architecture.md`` for the dependency rule.
"""
